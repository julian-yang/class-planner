var level = require('level');
var sublevel = require('level-sublevel');

module.exports.printMajors = function(db) {
    /*var majorDb = db.sublevel('majors');
    majorDb.createReadStream()
        .on('data', function(data) {
            console.log('key: %s value: %s', data.key, data.value);
        })
        .on('error', function(error) {
            console.log('printMajors: ' + error);
        });
        */
};
